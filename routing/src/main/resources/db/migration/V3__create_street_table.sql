CREATE TABLE "street" (
    "fromId" INT,
    "toId" INT
);

INSERT INTO "street" (fromId, toId)
VALUES (?, ?),
       (?, ?),
       (?, ?),
       (?, ?),
       (?, ?),
       (?, ?),
       (?, ?),
       (?, ?);
