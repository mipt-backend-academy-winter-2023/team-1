CREATE TABLE "building" (
    "id" INT,
    "longitude" FLOAT,
    "latitude" FLOAT,
    "name" VARCHAR
);

-- INSERT INTO "building" (id, longitude, latitude, name)
-- VALUES (1001, ?, ?, ?)
--        (1002, ?, ?, ?)
--       (1002, ?, ?, ?);
