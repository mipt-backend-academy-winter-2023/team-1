CREATE TABLE "crossroad" (
    "id" INT,
    "longitude" FLOAT,
    "latitude" FLOAT,
);

-- INSERT INTO "crossroad" (id, longitude, latitude)
-- VALUES (1, ?, ?),
--        (2, ?, ?),
--        (3, ?, ?);
